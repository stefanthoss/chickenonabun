version: "2"
plugins:
  bundler-audit:
    enabled: true
  csslint:
    enabled: true
  eslint:
    enabled: true
    config:
      config: .eslintrc
  markdownlint:
    enabled: true
  rubocop:
    enabled: true
